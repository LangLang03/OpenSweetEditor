# @opensweeteditor/widget

Browser widget layer for OpenSweetEditor Web SDK v2.

This package provides the browser-side rendering/input widget used by `@opensweeteditor/sdk`.

Most integration scenarios should use `@opensweeteditor/sdk` directly.

