{
  "name": "@opensweeteditor/sdk",
  "version": "2.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./runtime/*": "./runtime/*"
  },
  "files": [
    "dist",
    "runtime"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json && node ../../scripts/prepare-sdk-runtime.mjs",
    "typecheck": "pnpm --filter @opensweeteditor/core run build && pnpm --filter @opensweeteditor/widget run build && tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "pnpm --filter @opensweeteditor/core run build && pnpm --filter @opensweeteditor/widget run build && vitest run"
  },
  "dependencies": {
    "@opensweeteditor/core": "workspace:*",
    "@opensweeteditor/widget": "workspace:*"
  }
}
